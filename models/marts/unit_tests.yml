version: 2

unit_tests:
  - name: test_authorized_visits_15min_apart_same_location
    description: "Two authorized attempts on the same charger 15 minutes apart should result in one visit"
    model: fact_visits
    overrides:
      macros:
        is_incremental: false
    given:
      - input: ref('fact_charge_attempts')
        format: sql
        rows: |
          select 
              'CH-001' || '1' || '2025-10-02 10:00:00' as charge_attempt_id,
              'CH-001' as charge_point_id,
              '1' as connector_id,
              cast('2025-10-02 10:00:00' as timestamp) as ingested_ts,
              cast(null as timestamp) as charge_attempt_ingested_ts,
              cast(null as string) as charge_attempt_unique_id,
              cast(null as string) as previous_status,
              cast(null as string) as status,
              cast(null as string) as next_status,
              ['TAG-001'] as id_tags,
              ['Accepted'] as id_tag_statuses,
              'TXN-001' as transaction_id,
              cast(null as timestamp) as transaction_start_ts,
              cast(null as timestamp) as transaction_stop_ts,
              cast(null as timestamp) as transaction_ingested_ts,
              cast(null as string) as transaction_stop_reason,
              cast(null as numeric) as meter_start_wh,
              cast(null as numeric) as meter_stop_wh,
              5.5 as energy_transferred_kwh,
              cast(null as array) as error_codes,
              true as is_successful,
              cast('2025-10-02 10:20:00' as timestamp) as incremental_ts
          union all
          select 
              'CH-002' || '1' || '2025-10-02 10:15:00' as charge_attempt_id,
              'CH-002' as charge_point_id,
              '1' as connector_id,
              cast('2025-10-02 10:15:00' as timestamp) as ingested_ts,
              cast(null as timestamp) as charge_attempt_ingested_ts,
              cast(null as string) as charge_attempt_unique_id,
              cast(null as string) as previous_status,
              cast(null as string) as status,
              cast(null as string) as next_status,
              ['TAG-001'] as id_tags,
              ['Accepted'] as id_tag_statuses,
              'TXN-002' as transaction_id,
              cast(null as timestamp) as transaction_start_ts,
              cast(null as timestamp) as transaction_stop_ts,
              cast(null as timestamp) as transaction_ingested_ts,
              cast(null as string) as transaction_stop_reason,
              cast(null as numeric) as meter_start_wh,
              cast(null as numeric) as meter_stop_wh,
              6.2 as energy_transferred_kwh,
              cast(null as array) as error_codes,
              true as is_successful,
              cast('2025-10-02 10:20:00' as timestamp) as incremental_ts
      - input: ref('stg_ports')
        format: sql
        fixture: stg_ports_fixture
    expect:
      rows:
        - {location_id: 'LOC-001', id_tag: 'TAG-001', charge_attempt_count: 2, total_energy_transferred_kwh: 11.7}

  - name: test_unauthorized_visits_15min_apart
    description: "Two unauthorized attempts on the same charger 15 minutes apart should result in two separate visits (unauthorized visits group by charge_point_id within 2 minutes)"
    model: fact_visits
    overrides:
      macros:
        is_incremental: false
    given:
      - input: ref('fact_charge_attempts')
        format: sql
        rows: |
          select 
              'CH-001' || '1' || '2025-10-02 11:00:00' as charge_attempt_id,
              'CH-001' as charge_point_id,
              '1' as connector_id,
              cast('2025-10-02 11:00:00' as timestamp) as ingested_ts,
              cast(null as timestamp) as charge_attempt_ingested_ts,
              cast(null as string) as charge_attempt_unique_id,
              cast(null as string) as previous_status,
              cast(null as string) as status,
              cast(null as string) as next_status,
              [] as id_tags,
              [] as id_tag_statuses,
              'TXN-003' as transaction_id,
              cast(null as timestamp) as transaction_start_ts,
              cast(null as timestamp) as transaction_stop_ts,
              cast(null as timestamp) as transaction_ingested_ts,
              cast(null as string) as transaction_stop_reason,
              cast(null as numeric) as meter_start_wh,
              cast(null as numeric) as meter_stop_wh,
              3.0 as energy_transferred_kwh,
              cast(null as array) as error_codes,
              false as is_successful,
              cast('2025-10-02 11:20:00' as timestamp) as incremental_ts
          union all
          select 
              'CH-001' || '1' || '2025-10-02 11:15:00' as charge_attempt_id,
              'CH-001' as charge_point_id,
              '1' as connector_id,
              cast('2025-10-02 11:15:00' as timestamp) as ingested_ts,
              cast(null as timestamp) as charge_attempt_ingested_ts,
              cast(null as string) as charge_attempt_unique_id,
              cast(null as string) as previous_status,
              cast(null as string) as status,
              cast(null as string) as next_status,
              [] as id_tags,
              [] as id_tag_statuses,
              'TXN-004' as transaction_id,
              cast(null as timestamp) as transaction_start_ts,
              cast(null as timestamp) as transaction_stop_ts,
              cast(null as timestamp) as transaction_ingested_ts,
              cast(null as string) as transaction_stop_reason,
              cast(null as numeric) as meter_start_wh,
              cast(null as numeric) as meter_stop_wh,
              2.5 as energy_transferred_kwh,
              cast(null as array) as error_codes,
              false as is_successful,
              cast('2025-10-02 11:20:00' as timestamp) as incremental_ts
      - input: ref('stg_ports')
        format: sql
        fixture: stg_ports_fixture
    expect:
      rows:
        - {location_id: 'LOC-001', id_tag: null, charge_attempt_count: 1}
        - {location_id: 'LOC-001', id_tag: null, charge_attempt_count: 1}

  - name: test_unauthorized_visits_1min_apart
    description: "Two unauthorized attempts on the same charger 1 minutes apart should result in one visit (unauthorized visits group by charge_point_id within 2 minutes)"
    model: fact_visits
    overrides:
      macros:
        is_incremental: false
    given:
      - input: ref('fact_charge_attempts')
        format: sql
        rows: |
          select 
              'CH-001' || '1' || '2025-10-02 11:00:00' as charge_attempt_id,
              'CH-001' as charge_point_id,
              '1' as connector_id,
              cast('2025-10-02 11:00:00' as timestamp) as ingested_ts,
              cast(null as timestamp) as charge_attempt_ingested_ts,
              cast(null as string) as charge_attempt_unique_id,
              cast(null as string) as previous_status,
              cast(null as string) as status,
              cast(null as string) as next_status,
              [] as id_tags,
              [] as id_tag_statuses,
              'TXN-003' as transaction_id,
              cast(null as timestamp) as transaction_start_ts,
              cast(null as timestamp) as transaction_stop_ts,
              cast(null as timestamp) as transaction_ingested_ts,
              cast(null as string) as transaction_stop_reason,
              cast(null as numeric) as meter_start_wh,
              cast(null as numeric) as meter_stop_wh,
              3.0 as energy_transferred_kwh,
              cast(null as array) as error_codes,
              false as is_successful,
              cast('2025-10-02 11:20:00' as timestamp) as incremental_ts
          union all
          select 
              'CH-001' || '1' || '2025-10-02 11:15:00' as charge_attempt_id,
              'CH-001' as charge_point_id,
              '1' as connector_id,
              cast('2025-10-02 11:01:00' as timestamp) as ingested_ts,
              cast(null as timestamp) as charge_attempt_ingested_ts,
              cast(null as string) as charge_attempt_unique_id,
              cast(null as string) as previous_status,
              cast(null as string) as status,
              cast(null as string) as next_status,
              [] as id_tags,
              [] as id_tag_statuses,
              'TXN-004' as transaction_id,
              cast(null as timestamp) as transaction_start_ts,
              cast(null as timestamp) as transaction_stop_ts,
              cast(null as timestamp) as transaction_ingested_ts,
              cast(null as string) as transaction_stop_reason,
              cast(null as numeric) as meter_start_wh,
              cast(null as numeric) as meter_stop_wh,
              2.5 as energy_transferred_kwh,
              cast(null as array) as error_codes,
              false as is_successful,
              cast('2025-10-02 11:20:00' as timestamp) as incremental_ts
      - input: ref('stg_ports')
        format: sql
        fixture: stg_ports_fixture
    expect:
      rows:
        - {location_id: 'LOC-001', id_tag: null, charge_attempt_count: 2}

  - name: test_infer_id_tag_1min_apart
    description: "Should infer id from the second authorised attempt if close in time (less than 2 minutes on the same charger)"
    model: fact_visits
    overrides:
      macros:
        is_incremental: false
    given:
      - input: ref('fact_charge_attempts')
        format: sql
        rows: |
          select 
              'CH-001' || '1' || '2025-10-02 11:00:00' as charge_attempt_id,
              'CH-001' as charge_point_id,
              '1' as connector_id,
              cast('2025-10-02 11:00:00' as timestamp) as ingested_ts,
              cast(null as timestamp) as charge_attempt_ingested_ts,
              cast(null as string) as charge_attempt_unique_id,
              cast(null as string) as previous_status,
              cast(null as string) as status,
              cast(null as string) as next_status,
              [] as id_tags,
              [] as id_tag_statuses,
              'TXN-003' as transaction_id,
              cast(null as timestamp) as transaction_start_ts,
              cast(null as timestamp) as transaction_stop_ts,
              cast(null as timestamp) as transaction_ingested_ts,
              cast(null as string) as transaction_stop_reason,
              cast(null as numeric) as meter_start_wh,
              cast(null as numeric) as meter_stop_wh,
              3.0 as energy_transferred_kwh,
              cast(null as array) as error_codes,
              false as is_successful,
              cast('2025-10-02 11:20:00' as timestamp) as incremental_ts
          union all
          select 
              'CH-001' || '1' || '2025-10-02 11:15:00' as charge_attempt_id,
              'CH-001' as charge_point_id,
              '1' as connector_id,
              cast('2025-10-02 11:01:00' as timestamp) as ingested_ts,
              cast(null as timestamp) as charge_attempt_ingested_ts,
              cast(null as string) as charge_attempt_unique_id,
              cast(null as string) as previous_status,
              cast(null as string) as status,
              cast(null as string) as next_status,
              ['TAG-001'] as id_tags,
              ['Accepted'] as id_tag_statuses,
              'TXN-004' as transaction_id,
              cast(null as timestamp) as transaction_start_ts,
              cast(null as timestamp) as transaction_stop_ts,
              cast(null as timestamp) as transaction_ingested_ts,
              cast(null as string) as transaction_stop_reason,
              cast(null as numeric) as meter_start_wh,
              cast(null as numeric) as meter_stop_wh,
              2.5 as energy_transferred_kwh,
              cast(null as array) as error_codes,
              false as is_successful,
              cast('2025-10-02 11:20:00' as timestamp) as incremental_ts
      - input: ref('stg_ports')
        format: sql
        fixture: stg_ports_fixture
    expect:
      rows:
        - {location_id: 'LOC-001', id_tag: 'TAG-001', charge_attempt_count: 2}

  - name: test_unauth_auth_auth_visit
    description: "Should infer id from the second authorised attempt if close in time (less than 2 minutes on the same charger) and join to other attempts in the visit"
    model: fact_visits
    overrides:
      macros:
        is_incremental: false
    given:
      - input: ref('fact_charge_attempts')
        format: sql
        rows: |
          select 
              'CH-001' || '1' || '2025-10-02 11:00:00' as charge_attempt_id,
              'CH-001' as charge_point_id,
              '1' as connector_id,
              cast('2025-10-02 11:00:00' as timestamp) as ingested_ts,
              cast(null as timestamp) as charge_attempt_ingested_ts,
              cast(null as string) as charge_attempt_unique_id,
              cast(null as string) as previous_status,
              cast(null as string) as status,
              cast(null as string) as next_status,
              [] as id_tags,
              [] as id_tag_statuses,
              'TXN-003' as transaction_id,
              cast(null as timestamp) as transaction_start_ts,
              cast(null as timestamp) as transaction_stop_ts,
              cast(null as timestamp) as transaction_ingested_ts,
              cast(null as string) as transaction_stop_reason,
              cast(null as numeric) as meter_start_wh,
              cast(null as numeric) as meter_stop_wh,
              3.0 as energy_transferred_kwh,
              cast(null as array) as error_codes,
              false as is_successful,
              cast('2025-10-02 11:20:00' as timestamp) as incremental_ts
          union all
          select 
              'CH-001' || '1' || '2025-10-02 11:15:00' as charge_attempt_id,
              'CH-001' as charge_point_id,
              '1' as connector_id,
              cast('2025-10-02 11:01:00' as timestamp) as ingested_ts,
              cast(null as timestamp) as charge_attempt_ingested_ts,
              cast(null as string) as charge_attempt_unique_id,
              cast(null as string) as previous_status,
              cast(null as string) as status,
              cast(null as string) as next_status,
              ['TAG-001'] as id_tags,
              ['Accepted'] as id_tag_statuses,
              'TXN-004' as transaction_id,
              cast(null as timestamp) as transaction_start_ts,
              cast(null as timestamp) as transaction_stop_ts,
              cast(null as timestamp) as transaction_ingested_ts,
              cast(null as string) as transaction_stop_reason,
              cast(null as numeric) as meter_start_wh,
              cast(null as numeric) as meter_stop_wh,
              2.5 as energy_transferred_kwh,
              cast(null as array) as error_codes,
              false as is_successful,
              cast('2025-10-02 11:20:00' as timestamp) as incremental_ts
          union all
          select 
              'CH-002' || '1' || '2025-10-02 11:15:00' as charge_attempt_id,
              'CH-002' as charge_point_id,
              '1' as connector_id,
              cast('2025-10-02 11:20:00' as timestamp) as ingested_ts,
              cast(null as timestamp) as charge_attempt_ingested_ts,
              cast(null as string) as charge_attempt_unique_id,
              cast(null as string) as previous_status,
              cast(null as string) as status,
              cast(null as string) as next_status,
              ['TAG-001'] as id_tags,
              ['Accepted'] as id_tag_statuses,
              'TXN-004' as transaction_id,
              cast(null as timestamp) as transaction_start_ts,
              cast(null as timestamp) as transaction_stop_ts,
              cast(null as timestamp) as transaction_ingested_ts,
              cast(null as string) as transaction_stop_reason,
              cast(null as numeric) as meter_start_wh,
              cast(null as numeric) as meter_stop_wh,
              2.5 as energy_transferred_kwh,
              cast(null as array) as error_codes,
              false as is_successful,
              cast('2025-10-02 11:20:00' as timestamp) as incremental_ts              
      - input: ref('stg_ports')
        format: sql
        fixture: stg_ports_fixture
    expect:
      rows:
        - {location_id: 'LOC-001', id_tag: 'TAG-001', charge_attempt_count: 3}