name: why_downtime_check
prompt: "Why is my charging network experiencing downtime?"
sql: |
  SELECT
    reason,
    SUM(duration_minutes) AS total_downtime_minutes
  FROM analytics.ANALYTICS.fact_downtime_daily
  GROUP BY reason
  ORDER BY total_downtime_minutes DESC
